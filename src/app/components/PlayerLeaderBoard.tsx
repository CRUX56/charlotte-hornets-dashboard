import { PlayerData, PlayerLeaderBoardProps } from "@/lib/models";

function PlayerLeaderBoard({ teamRoster }: { teamRoster: PlayerData[] }) {
  return (
    <div>
      <h2>Player Leaderboard</h2>
      {/* Render the leaderboard here */}
    </div>
  );
}
export default PlayerLeaderBoard;
